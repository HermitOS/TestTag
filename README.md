# Test repo for verifying the tag actions

This repo checks the tag actions for checking and  pushing deploy tags.

It also checks the CI build test coverage. For CRAP score defaults, color bands, and how to override thresholds in a workflow, see [.github/actions/SimpleCodeCoverage/Readme.md](.github/actions/SimpleCodeCoverage/Readme.md).

